{
  "cicd-mcp": {
    "type": "stdio",
    "command": "npx",
    "args": ["-y", "@modelcontextprotocol/server-gitlab"],
    "env": {
      "GITLAB_TOKEN": "${{ secrets.COPILOT_CICD_TOKEN }}",
      "GITLAB_URL": "${{ secrets.COPILOT_CICD_ORG }}"
    }
  }
}
