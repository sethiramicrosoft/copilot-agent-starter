{
  "cicd-mcp": {
    "type": "stdio",
    "command": "npx",
    "args": ["-y", "@modelcontextprotocol/server-github-actions"],
    "env": {
      "GITHUB_TOKEN": "${{ secrets.COPILOT_CICD_TOKEN }}",
      "GITHUB_ORG": "${{ secrets.COPILOT_CICD_ORG }}"
    }
  }
}
