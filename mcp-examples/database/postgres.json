{
  "db-mcp": {
    "type": "local",
    "command": "npx",
    "args": [
      "-y",
      "@modelcontextprotocol/server-postgres"
    ],
    "tools": [
      "*"
    ],
    "env": {
      "POSTGRES_CONNECTION_STRING": "${{ secrets.COPILOT_DB_CONNECTION_STRING }}"
    }
  }
}
